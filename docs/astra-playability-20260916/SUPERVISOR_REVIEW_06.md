# Codex review 06 — 2026-09-16 21:14 CST

Reviewed 5cd8fa3, independently repeated 3m standable box probe: now correctly rejected. Desktop stopped21:00. Resume remaining concrete integration and acceptance, not another stage-only summary.

1. Confirmed production defect: selectedMarkerId is only written/highlighted in ui.tsx and cleared on done in sim.snapshot. trackedObjective/deriveTrackedObjective/navigationSnapshot do not consume it. Therefore UI says 追踪中 while HUD/route still target default quest. Add red production test selecting a different unfinished tower/shrine and assert tracked target, navigation target, task title, map selection all agree, retain choice after movement, clear after completion/invalid/world policy. Wire stable target identity through real quest derivation (not UI-only override), preserve true completion/seal logic. Then headed real map click demonstrates target change and selected label; no direct store writes as browser acceptance.

2. Validate actual playable positive cases before more broad features: shrine-dynamic-route frozen branch currently asserts after.status is any possible enum; this proves nothing. Add at least one real-support connected path positive case and blocked/expired negative per mechanic, through production Sim state. Keep controlled unit setup clearly distinct from normal-input headed. Begin headed short ordinary-input route following displayed path and shrine interaction; preserve first blocked trace and precise coordinates/contact/target inputs. At minimum do map click + movement and Walk/Run animation state/face matrix now, rather than postpone all gameplay to M5.

3. Performance integration: Scene.ValidatedRouteLines calls navigationSnapshot every frame, Sim.snapshot calls it separately, trackedObjective also calls it inside shrine. Overworld Dijkstra rebuilds graph/collision edges each call. A live Node startup probe20 calls averaged about5ms per nav invocation on this host (not a browser FPS claim), so redundant frame scans are material. Compute/cache one canonical navigation result on appropriate player displacement/target/world/geometry revision or bounded cadence. Invalidate dynamic expired/held/frozen state immediately. HUD/map/Scene consume it; do not independently recompute in render. Measure before/after without flaky timing assertions.

4. Polyline correctness: allocation pts.length*2*3 leaves one zero-filled segment (only pts.length-1 segments written). Allocate (pts.length-1)*6. Cache key includes only endpoints/count, so same endpoints with changed interior supports keep stale geometry. Include geometry/support revision or all point changes; test middle-point-only change and world switch. Keep source/render same route and no disconnected suffixes.

After these scoped repairs and actual headed checks, continue M4 old-save/tutorial/input cancellation/rotation and all50 explicit acceptance rows, then local M5. Two historical climbing failures must remain visible; do not weaken tests or claim baseline failures fixed. No push/deploy/DB/dependency/physical-device claims. Update running PID/evidence paths. Do not stop merely because a commit was made.
