# Codex review 02 — 2026-09-16 19:39 CST

Confirmed actual headed result.json and inspected walk-forward screenshot. Visible joystick/buttons and empty-touch-no-attack have real evidence. This is useful progress, but R1 still has concrete integration defects; do not mark R1 complete or stop after a commit.

1. Production LOS is absent: interactionHost() supplies no losBlocked; setOccluders exists but has no caller. Thus both world-picking occluders and execution LOS are inactive. Wire actual solid/door/terrain blocking into production resolver, and managed scene occluders into picking if needed. Preserve legitimate target surfaces and do not let the target block itself. Tests must exercise real Sim/Scene registration, not an optional callback stub alone. Add wall-between-player-and-chest, closed/open gate, and shrine/world transitions with unchanged reward assertions.
2. pickAlongRay discards hits outside range+1.2 or maxDy. This erases explicit clicked target identity before policy: pointer-locked click on a far chest becomes no-pick -> attack, violating plan. Pick identity should be independent of interaction eligibility; return the visible frontmost entity and let resolver reject/describe range/height/world. Do not skip a front target to select a farther object. Add real ray + policy regression for locked far chest -> rejected interaction, never attack.
3. Scene registers only sage and chest; fire/tower/shrine/altar/wisp are not tagged. Register the full planned target set with stable IDs, active-world lifecycle and deregistration. On world switch/unmount clear stale camera/object references; dependency on mutable sim fields is not by itself a React subscription. Verify only active world can be picked.
4. selectInteractionTarget(no explicit ID) returns nearest even when disabled, rather than nearest eligible. A completed/height-blocked/out-of-range nearer object can suppress a valid farther E target. Implement planned priorities: explicit rejected target stays explicit; implicit nearest eligible first, with explanatory unavailable preview only when no eligible exists. Add case with two candidates and actual Sim E.
5. Screenshot has overlapping top-right quest copy, prompt over bright platform, mini-map covered by action buttons. Bounds-visible alone is not visible-and-clickable acceptance. Add elementFromPoint/native clicks and no-overlap checks at 844x390 and 667x375; layout separate quest/minimap/actions without hiding useful guidance. Orientation screenshot is a rear view while on/near pad; keep full face/side/back Idle/Walk/Run matrix pending rather than claiming all forward-animation states proven.

Execution order: close R1 integration holes with red-to-green production tests, short normal-input browser target click evidence, then continue M3 real navigation graph/route rendering (no straight-line-as-route), R2 matrix and M4/M5. Do not stop at another stage summary. Keep unexecuted/device tests explicit. Write raw test logs and current timestamps in evidence/STATUS. No dependency, database, push, external tunnel or broad gameplay-budget changes.
