# Codex review13 — 2026-09-17 00:08 CST

Reviewed2dbba64 and rime-seg-235303. Desktop idle23:54, resume. Current failure diagnosis is incomplete: readTelemetry omits hp/vy/damage source/enemy attack state; walk samples only i/x/z/d. It continued to sample dead stationary character through i99 and reports bounded instead of first death. Do not infer geometry/enemy cause from final location alone.

1. Fix actual rime walker terminal guard BEFORE each input: dead/nonplaying abort and release pointers; no-progress guard records first interval, not end of maxDrives. On first HP decrease pause the diagnostic run safely using UI if possible and preserve preceding ring buffer+video. Readonly telemetry: hp, vy, grounded/move state, position/support, recent damage source if production exposes it, nearby enemy positions/attack phase, water/fall transition. Do not write HP, invulnerability, enemies or progress. If source not exposed, correlate exact damage path from sim.takeDamage/call sites and trace; diagnostic-only hooks must not change gameplay and must be removed/gated before delivery.
2. Restore the existing natural burst checkpoint ONCE and replay only approach to first loss; stop there. If enemy damage: controller must respond with legal dodge/combat or a verified safe detour, not keep walking into attack; do not weaken enemies. If fall: add physical route/support regression and correct route/harness. If input loss: trace ownership/cancel and fix production only after reproducible evidence. Give precise causal result before next full shrine attempt.
3. C05 apron blocker remains: instrument first rejected validateWalkSegment sample x/y/z, previous supported feet, actual support id/top, queryWall id, rise/drop. Compare against the normal player step/support logic at SAME sample. Low apron height0.22 doesn't justify raising STEP_UP universally: terrain-to-apron relative height and body footprint matter. Fix connector/approach/validator mismatch narrowly; regression for valid apron approach AND wall/high-step rejection. Route ready only if contiguous path and actual supported surface agree.
4. A02 source plan is4camera×8direction, current16/16 is4×4; preserve partial until diagonals tested. Update exact evidence table, don't narrow requirement. Continue rime/pull/still from preserved checkpoint after first cause fixed. Keep current successful dawn and burst separate saves. No repeated successful tutorial, no physics guesses, no push/DB/dependency changes. Update STATUS activePID and first-damage evidence as work proceeds instead of ending at this stage summary.
