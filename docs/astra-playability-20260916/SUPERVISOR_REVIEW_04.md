# Codex review 04 — 2026-09-16 20:29 CST

Verified e767b37, reviewed production changes, independently ran review03-integration + navigation: 19/19 pass. This accepts those narrow regressions, not M3 completion. Desktop is idle at 20:16. Resume with the implementation below, rather than another status-only pass.

## This work unit: real M3 production navigation, first one complete shrine then all four

Concrete disconnected code: buildShrineInteriorGraph exists but sim.trackedObjective returns null route fields immediately for shrine world. The new graph has only entrance/exit/altar and mechanismSolved boolean. That cannot represent actual ice platforms, movable boards, broken wall, or time-limited moving/frozen block. Its only altar edge is a straight line, not a validated support path. Renaming status approximate was a necessary honesty fix, not implementation of the user's plan.

1. Read plan sections 6.4–6.8 and actual shrine mechanism/solids/support code. Pick burst shrine as first bounded integration: actual crackedBroken state is available in Sim. Identify entrance-side approach, wall/action point, passage, altar approach and exit anchors from live world constants after initialization. Keep exact world IDs.
2. Add failing production tests calling trackedObjective/navigation snapshot in burst before wall breaks, after it breaks, and after exit. Before: route walks only to safe action anchor and displays actual required art; after: route updates to altar via valid passage; after exit: no stale shrine segment. No generic fake mechanismSolved fixture as sole coverage.
3. Wire active-world navigation through sim/store. Represent route segments, status/reason, next action, and world ID. Validate local connectors using current solids, support heights, capsule clearance and drop limits from existing physics; use intermediate waypoints rather than arbitrary hub chords. A rejected connector gives explicit blocked reason; never draw it across a wall/pit. Retain approximate bearing for genuinely unvalidated portions only.
4. Feed one route snapshot into task card and map/compass/world route renderers (existing Scene/ui/store). Draw only verified walk segments slightly above support; break at climb/skill/gap actions. Add selected target on map using stable target ID; selection persists until completed/invalid/world changes, not nearest target flicker. Do not put developer text such as 未验证走廊 into final player UI; use actionable natural text like 前往机关附近 / 尚未找到可通行路线.
5. Verify first burst integration via normal input headed, or a clearly identified legitimate existing save restored once. Do not create fake progress/teleport. If browser route blocks, retain first failing trace and diagnose narrowly; do not repeatedly run whole route or claim the fixture as player acceptance. Continue implement rime/pull/still using their real support and dynamic timers; frozen expiry must invalidate the segment. Full overworld tower/shrine/citadel approach geometry must follow same contract.

## Evidence cleanup and next work
ACCEPTANCE currently overlaps C02–C12 and lists 50 IDs as grouped ranges without requirements; expand one row per exact original ID and original requirement. Mark C06–C09 implementation incomplete until production integration (not merely code layer). Keep source-stage test counts separate: latest narrow verified19/19; MiMo full game388/390 needs raw log. STATUS must have one current summary and explicit history, not conflicting active sections374/376 plus388/390. Local timezone is Asia/Shanghai, not Tokyo. Record actual running PIDs and reap your own servers.

After real M3, do pending Walk/Run/action orientation and M4 regressions, then local M5 packaging/source mapping. Existing climb trace is useful but hypothesis remains unproven: do not claim mantle cause from peak/final alone. If touching climb, capture contact/support/mantle phase around first bad transition, preserve assertions/budgets. Do not push/deploy/DB/dependency edits. Do not stop after committing this work unit while authorized dependent tasks remain; update progress and continue.
