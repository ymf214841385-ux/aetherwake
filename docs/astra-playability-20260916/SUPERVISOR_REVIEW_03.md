# Codex review 03 — 2026-09-16 20:05 CST

Live reviewed f12aba8 plus production source; desktop ended at 19:53 although substantial scope remains. Resume actual work now. Preserve EXECUTION_BRIEF constraints; only isolated tree, no push/deploy/DB/dependency changes. Commit boundaries are not completion. Process the following bounded repairs in order, then continue remaining plan without awaiting another check.

## 1. Actual LOS regression introduced by broad exemption
`interaction.ts` STRUCTURE_ACTIONS skips LOS for activate/enter/leave/claim/reset. This is not target-self-occlusion handling: any independent wall between player and altar/door/tower is now ignored. `sim.interactionHost` also discards every solid within 0.8 of target or 0.6 of player, including unrelated blocking doors.
First add failing production Sim tests for wall-separated shrine enter / altar claim / tower activation in valid range and height, unchanged world/reward, then remove wall and prove valid interaction. Replace action-category/radius exemptions with explicit target surface/interaction anchor and exclusion of only identified target-owned collision, or an exterior reachable anchor. Trace actual solids IDs and coordinate conventions first; do not invent IDs or blindly exclude whole structure. Closed door remains blocker. Keep chest LOS test and no reward duplicate.

## 2. M3 graph is explicitly placeholder, not usable route
`navigation.ts`: tower-base and tower-top have identical x/y/z from t.y; climb cost fixed12; all edges directed outward from spawn/sage, no traversable return edges; nearestNodeId ignores y/world/blocked connectivity. `trackedObjective` calls ready costs route distance even though no corridor geometry checked. Example: origin at a shrine node has no outgoing edges => unreachable to another shrine; near destination snaps to it => cost0 although player may be far/behind wall; tower top cannot be distinguished from base.
Red tests must reproduce these cases through actual trackedObjective, not merely graph node existence. Then author real ground approach waypoints using world height/solids and existing tower ledge/mantle geometry. Separate true base/top heights; validate connectors from current player position, include connector length; reverse only genuinely traversable walk edges, preserve directed climbs/drops. No fabricated direct path through terrain or walls. Until validated, label unavailable/approximate bearing, never claim a traversable route. Add shrine interior world-specific nodes and dynamic edges based on actual mechanism state, as original plan specifies. One canonical route should drive HUD/map/compass/ground indication. Verify one normal-input headed route and a closed/open mechanism change before expanding.

## 3. Missing active-world picking remains
Scene tags cover overworld only; actual candidate IDs altar:<id> and exit:<id> are absent from rendered shrine meshes. ShrineRooms creates altar without interactId. Add actual altar/exit pick surfaces, correct shrine world IDs, and active-world registration. Verify mounted scene registration across enter -> altar/exit -> return, stale objects cannot resolve; mutable sim fields in effect dependencies alone are not proof of React updates. Add tests for real production registration/command path, not only synthetic callbacks.

## Continue / acceptance
After these fixes complete pending Walk/Run and action orientation evidence, remaining M4 and local M5. Keep physical phone pending only for genuine device checks. Do not weaken two historical climb assertions or increase frame budgets to hide failure. For any attempted climb repair first retain short failing trace with position/stamina/contact/state and identify production cause. Update ACCEPTANCE with actual evidence for all 50 items. Record raw logs, exact source commit, and unfinished items. Stage summaries are progress reports; continue work rather than stop while executable tasks remain.
