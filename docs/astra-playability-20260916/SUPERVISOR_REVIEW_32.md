# R32 — two remaining verifier errors + visible close layout

Independent R31 audit: real landing now grounded/hp4/non-glide accepted. C09 strict CODE test now tests walk→action-required; C08 exact support ID accepted as CODE only; C06 remains lifetime-only, correctly partial. Do not redo these passed portions.

Two concrete remaining errors in `scripts/qa/r31-headed-accept.mjs`:
1. lines87-102 `touchStickDrag` uses page.mouse after touchscreen.tap and comments that hasTouch converts mouse into touch. This is false: hasTouch enables touch capability; page.mouse is mouse input. Reuse your prior real multi-touch browser protocol helper (Input.dispatchTouchEvent touchStart/move/end via browser transport). Do not dispatch synthetic DOM pointer events. Add passive observer recording pointerType/isTrusted/pointerId on actual joystick events, assert movement-producing stream pointerType='touch', consistent id, down/move/up and no stuck input afterward. Actual touch remains emulation, phone still0. Fix comments. No keyboard/mouse fallback in D11.
2. lines244-268 A06-steady-facing is measured AFTER landing. Therefore does not prove glide facing. Use existing airborne trace, save full trace and compute settled glide velocity/facing sample(s) while state==='gliding', speed>0.2; one ordinary in-air camera/input direction change and enough settle time. Ground facing is a separate metric. Keep actual safe landing assertion. Do not claim air-facing from grounded data. If cannot obtain stable airborne samples, retain partial instead of inventing pass.

Actual user-facing issue noted in ACCEPTANCE: close button offscreen at844x390. Inspect src/styles.css modal rules and ui.tsx Modal layout. First capture bbox of dialog header/button and viewport, reproduce. Minimal CSS layout fix (bounded viewport height, reachable header close, scroll body) so visible touch close is reachable at844x390 AND667x375, no backdrop-only workaround. Test actual touch close, no world attack bleed and touch joystick after close. Keep backdrop behavior if intended. No invented new scope: this is existing small-screen critical-control requirement.

Only MiMo writes source. No push/deploy/tunnel/dependency changes. Do not run full mainline. At most one run after the above diagnosed fixes, inspect actual failure before retry. Do NOT repackage every attempt; finalize checks/package only once remaining verification settles. Update ACCEPTANCE honest proof layers, preserve existing raw evidence, don't rewrite it. If production CSS changes run typecheck/build:app plus affected input checks; rerun relevant strict tests. Existing historical 2game/16scripts remain disclosed.
