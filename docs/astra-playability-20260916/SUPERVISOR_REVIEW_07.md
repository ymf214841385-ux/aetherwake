# Codex review 07 — 2026-09-16 21:35 CST

Reviewed5bb44d2 and r6-headed2 result: real map click switches quest/nav to mere. Independently ran review06-map-nav + m4-save-compat14/14 pass. PID78593 is exited. Desktop idle21:18. Resume the next acceptance unit now. Local packaging is ALREADY authorized by EXECUTION_BRIEF; only publishing/deployment is outside this round. Do not wait for authorization to do local M5.

## 1 Final cache correctness before gameplay
navCacheKey misses ice.y, metal.y, plank state, moving block coordinates, and actual solid geometry (only solids.length). Thus unchanged count with moved support can reuse stale walk segments. Introduce geometry/support revision updated by actual mutation paths or complete relevant position/support signatures; retain cheap cache. Add regression: only metal height changes, same-count solid moves across path, plank moves, current target completes while another replaces set member at equal size => route invalidates. Use real production mutations where available; don't only assert arbitrary key strings. Preserve expiry invalidation.

## 2 Execute actual short gameplay, not another HUD text check
Follow original plan11.7 short loop: title/new game → visible controls move/look → sage conversation → initial chest reward → map select dawn → follow guidance to tower base → climb/activate → task update → reload and check saved progress. Use ordinary input with readonly sim telemetry, no writes/teleports/claim shortcuts. Use existing headed lifecycle and retain video/screenshots plus first failing trace. Segment by milestone; stop first failure, explain exact state/input/collision and fix narrowly before rerunning only affected segment. Historical climb issues must not be skipped if this loop hits them. Do not start full mainline repeatedly. Show actual animation state/clip, facing and displacement for walk/run; speed alone is not orientation acceptance. Preserve first failure and bounded runtime; no bigger time budgets as repair.

## 3 M4 is broader than three save tests
Read original50 requirements and write ONE row per exact ID with requirement, source commit, test/evidence path, status. Current ACCEPTANCE is still20:22 with duplicate C ranges and blank descriptions; it cannot track completeness. Run local feasible D/B regressions: three simultaneous touches move+look+action, pointercancel/blur/page hidden/rotation -> no stuck input, UI map/bag/dialogue opening clears owned inputs, bow cancel, touch shorttap vs drag. Start with existing input tests + headed native events, do not label desktop simulation as phone. Mark unavailable actual device explicitly; continue all local work.

## 4 Local M5 after repairs
Run final appropriate game/QA/typecheck/build:app with raw logs. Make local source archive with exact source commit and formal GLB SHA manifest, excluding deps/secrets/runtime profile; reopen archive and compare key source hashes. Report known2 climb failures and every not-yet-accepted requirement. No deploy/push needed or authorized. If gameplay has unresolved blockers, deliver checkpoint with blockers, never call full acceptance. Continue remaining implementation per plan, not stop at a partial commit. Update STATUS running PID and evidence milestones so next check can distinguish running from finished.
