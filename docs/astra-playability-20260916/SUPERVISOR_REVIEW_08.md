# Codex review 08 — 2026-09-16 21:57 CST

Urgent correction of acceptance evidence. Reviewed d738294 and short-loop5 JSON plus actual short-loop.mjs. Do not call this summit failure or required short-loop pass. Resume actual work.

## Proven test-harness root cause
short-loop.mjs lines251–255 sets cam.yaw=PI and player.yaw=PI claiming toward -Z. sim.ts922–923 uses forward=(-sin(yaw),-cos(yaw)); at PI forward is +Z. At tower approach player was(11.3577,13.5915,73.7974), tower target(10,68). After the supposed climb player was(11.2582,16.6011,114.7279), grounded with stamina100. This is walking AWAY from tower, not evidence of summit/mantle failure. Historical two unit failures exist separately; they cannot explain this trace without a real climb attempt.

## Required corrections before any more gameplay conclusions
1. Remove every direct cam.yaw/player.yaw assignment in this acceptance script (sage/chest/tower branches also mutate cam). Observability is readonly. Use genuine pointer/touch look movement and readonly yaw to converge on atan2(-dx,-dz). For +Z side tower approach desired yaw is near0, not PI. Add a small direction math regression or script assertion: proposed movement vector dot(target-player)>0 before driving; use progress-vs-distance samples to abort promptly if moving away. Do not repair by directly assigning correct yaw either.
2. Replace optional climb/activate with REQUIRED original11.7 milestones. ok must false for absent activation; require towersOn includes dawn from actual interaction and save/reload retains dawn. Entering climbing or y>30 alone is not tower completion. Mark existing report partial and update STATUS/ACCEPTANCE; do not overwrite historical raw JSON.
3. Inspect actual DOM climb affordance and input contract. Current script queries data-touch-action=climb and silently skips absent button, then taps interact. Record whether control exists and expected E/climb fallback at tower base. Move to real wall contact from safe approach, capture x/y/z, velocity, wish direction, state, wall ID/normal, stamina, mantle phase at short intervals. On first nonprogress/wrong-direction/contact failure stop and diagnose. Use native keyboard C only in explicitly desktop acceptance, or real touch control in touch acceptance. Do not fake DOM clicks on nonexistent buttons or suppress errors.
4. Run corrected bounded approach/climb from legitimate same-session/save state once. If real physics failure occurs, preserve first bad transition and write a focused failing Sim regression before source repair. Preserve mantle collision/reach/stamina/input contracts; no budget extensions or reward writes. If no usable retained checkpoint, one corrected short loop is justified because harness changed, not repeated unmodified loops.
5. Then finish outstanding orientation matrix, D07 rotation, E02 storage-failure tests, and update local package after actual source changes. Current archive is only partial checkpoint, not accepted completion. No push/deploy. Continue local work rather than stop after summary.
