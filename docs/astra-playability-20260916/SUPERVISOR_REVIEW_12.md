# Codex review12 — 2026-09-16 23:25 CST

Desktop stopped23:20 with Internal Server Error. Resume existing work; do not restart successful runs. Reviewed burst-seg4-231522 JSON: wall broken, nav walk, altar claim auto-exits to overworld, orbs1/shrinesOn burst persists after reload. PID88822 exited. This supports the burst segment, not a combined dawn+burst playthrough: afterClaim.towers=[] because this segment used fresh context. Preserve provenance; never merge save progress manually.

1. Update STATUS now with actual successful segment and retained checkpoint path/hash/source run. Keep independent dawn and burst results distinct. Continue from burst's natural storageState to next shrine segment, preserving original storage origin/port so Continue actually loads it. If no save loaded, fail clearly; don't silently replace intended resume with new game. Reuse checkpoint and stop at first failed milestone, no rerunning successful path on service retry.
2. Finish already-written orient-a02 and d07-rotate scripts and actual tutorial skip/replay UI check; run typecheck/build:app for pending ui.tsx/styles changes. Tutorial hint implementation must satisfy original prompt help requirements, not just a static text replacement; verify can skip/reopen and game input doesn't leak. Mark A02/D07 simulated/desktop evidence accurately.
3. Burst travel script steers directly toward hardcoded coordinates while nav unavailable (initial report). Therefore burst passage validates mechanics, NOT automatically C05 route guidance for overworld. Keep C05 partial and inspect why graph lacks a path along the actual traversed safe corridor. Add observed physically validated intermediate approach waypoints and route connector tests; ensure a normal player can follow guidance, not only a harness knowing world coordinates. Preserve obstacles; no fake straight-line-ready. Route-after-wall check should require concrete expected valid path/altar target, not allow any status other than action-required to pass.
4. Continue remaining rime/pull/still + towers/mainline milestones from legitimate saves after these bounded changes; E04 still partial until full mainline. Retain physical device pending. Two historical tests remain failures, no all-green claim. Source edits remain yours only. No publishing/DB/dependencies. Commit meaningful changes and refresh local archive only after validation. Do not end with another partial-stage summary while executable tasks remain.
