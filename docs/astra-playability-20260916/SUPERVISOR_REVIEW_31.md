# Review31 — reject false-positive acceptance conditions, fix verifier before more packaging

R30 result claims ok but actual assertions do not establish requested outcomes. Do not repackage until these are corrected. Preserve old raw evidence. No production source change unless a real defect is reproduced. Remain desktop MiMo only, no browser state writes, no physics/input budget changes.

## 1. D11 is not pure touch
`scripts/qa/r30-headed-accept.mjs` opens map/bag by tap but closes by keyboard Escape twice; resumes with KeyW. It therefore does not prove pure touch closure or joystick restoration. Use visible actual close/back UI with tap and real touch joystick drag to resume. If no reachable touch close exists, this is a user-facing defect: first reproduce it, implement minimal accessible close control using existing intended close action, add regression, verify normally. Separate desktop-keyboard checks from pure-touch acceptance. Do not use synthetic DOM handlers or state writes.
D08 pass currently only checks bagMode/afterMode. Add actual movedUnderBag≈0, attackPhase idle, resumedMoved>threshold checks; test map similarly. Current `.tap().catch(()=>{})` may swallow genuine control failures: only expected overlay interception can count blocked, and record actual attempted point/outcome. For isolation tap an observed onscreen world area under modal via physical touchscreen coordinate; assert no attack/damage/movement. Do not infer no-op from failed locator alone.

## 2. A06 landing is FALSE
`r30-accept-headed9/result.json` reports land.state='gliding', y28.945 and nonetheless A06-land-not-climb-pose pass=true. Script explicitly accepts gliding/dead as valid landing. Require grounded AND hp>0, glide=false, no climb, correct visual/animation after an actual safe landing. If flight exceeds bounded time record partial, not pass. Allow up to60s of normal input with closest safe landing, no teleport. Record actual trace across turn and stable motion; yaw merely changing is not alignment. Facing is (-sin(yaw),-cos(yaw)); compare with observed vx/vz after settling and existing formal model calibration, exclude speed≈0. Current first airborne transient does not establish steady facing. View resulting screenshots.

## 3. C06/C09 tests are vacuous for transition
`r30-expiry-support.test.ts` records before but never requires it valid; after action-required OR unavailable passes even if navigation never had a walk. Require active route over actual support before expiry, then blocked/replanned afterward, with concrete segments/points; use known aligned bridge geometry from prior successful still/rime evidence. Do not nudge player.x as a cache workaround: same pose must see support lifetime/geometry change. In-memory fixtures allowed but must label CODE layer, not headed. ACCEPTANCE currently labels combined claim+unit expiry Headed通过; split proof layer or keep Headed部分 for expiry until real input expiry witnessed. Timer reaching zero alone is not route invalidation.

## 4. C08 support assertion too permissive
`sup.id.includes('metal') || sup.y > o.y-2` can pass on any raised floor. Require exact plate support id and expected height; same coordinates without plate or held plate must lose that specific support. This proves local support only, not continuous crossing/navigation; do not claim bridge route. Either demonstrate ordinary grab/placement + actual crossing with safe closest natural save or explicitly retain blocked/partial and concrete geometry cause. Original Review30 requested mechanism diagnosis, not standalone canned support test.

Apply verifier corrections first; execute ONE bounded fresh run per case. On failure inspect first failing trace and provide source cause before retry, no nine blind repetitions. Save exact evidence, don't overwrite old run. Update STATUS/ACCEPTANCE truthfully. Final pack after results settle. Existing historical 2 game/16 scripts remain acknowledged; no need broad tests until source/test changes warrant. No push/deploy/tunnel.
