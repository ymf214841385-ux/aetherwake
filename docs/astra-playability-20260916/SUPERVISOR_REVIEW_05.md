# Codex review 05 — 2026-09-16 20:51 CST

Reviewed 8491188, production source and m3-hud4 result; server PID76141 exited. Desktop stopped at20:41 again. Resume this bounded correctness work before expanding navigation. Integration now exists, but validated currently overclaims physical reachability.

1. REPRODUCED live: validateWalkSegment from(0,0,0) to(0,0,4), heightFn=0, single solid {id:tall-standable-box,kind:box,x:0,y:0,z:2,w:2,h:3,d:1,standable:true} returns validated:true through a 3m box. shrine-route.ts ignores every standable solid in wall rejection. Standable top is not a passable side. Add this failing regression first. Use actual capsule clearance/step-height rules; reject side collision, uphill discontinuities and head clearance violations. supportY starts best at terrain height even above maxLift, so add explicit too-high terrain step and thin-wall/subsample tests; no magic upward snapping. Match production constants and swept checks rather than unverified STEP=.55/MAX_DROP=1.35 approximations.

2. All shrine builders use anyAltar/reached = some validated final segment. A disconnected final segment can mark whole route walk and filters discard blocked middle pieces. Fix to search/assemble a contiguous validated path rooted at the CURRENT player position; stop at first unavailable edge/action, never draw floating disconnected suffixes. Add red case first connector blocked but final altar segment clear => unavailable, no disconnected suffix. Also player already beyond entrance must not always walk back to entrance. Alternative branches require actual connected graph choice, not concatenating both.

3. Dynamic navigation fabricates duplicate support geometry. Sim.extraSupports already supplies ice y+3.35/r1.2, unheld metal y+.28/r1.15, and actual still slab in solids. shrine-route-dynamic independently adds ice at y/r.9 (including expired), metal y+.2 including held, frozen block y+.3/r1.6. These imaginary supports diverge from physical walkability. Consume one canonical live physics support list; use the same surface heights/world membership/lifetime/held state. Do not manufacture duplicate supports. Add expiration, held metal, wrong-world ice, and freeze-window invalidation tests through Sim.navigationSnapshot; baseline dynamic tests only check unsolved text and do not test playable connectivity. Jump actions must be represented as actions, not walk segments.

4. ValidatedRouteLines compares only IDs and starting a point, ignoring b/endpoints; moving target/support endpoint can remain visually stale. It renders straight endpoints even though validation sampled terrain, so line crosses hills/pits visually. Return full support-following polyline points, consume same cached snapshot used by HUD/map, invalidate on endpoint/world/support revision changes. Avoid duplicate full navigation+solid scans per frame in Scene and snapshot. Add endpoint-only change + frozen expiry tests and short headed visual evidence.

5. Current overworld approach is one direct chord; blocked start->tower has no detour search, so original plan M3 remains incomplete. Once validator/connectivity is sound, connect authored physically validated waypoints and use graph search for detours; retain honest unavailable on missing path. Do not stop with another renaming fix. Finish stable map selection and shared rendering, then pending orientation/M4/M5. Keep tests faithful; no physics/input/save/claim weakening, no push/deploy/dependency/DB. Raw logs and one accurate STATUS; current STATUS392/394 differs UI395/397, reconcile using actual last command.
