# Review 16 — 2026-09-17 01:35

HEAD 5ee5edf。自然完成档链已有新产物；继续第15轮未完成事项，先修确定的控制器错误，不重复跑整条失败路线。

1. pull-local.mjs 200-205、240 把世界撤离 dx/dz 直接传给屏幕 stick dx/dy，坐标系错误。sim.ts 925-930 实际使用 forward=(-sin yaw,-cos yaw)、right=(cos yaw,-sin yaw)。世界单位撤离向量 v 应转换 localX=dot(v,right)，localForward=dot(v,forward)，屏幕offset=(localX*40,-localForward*40)。两处共用转换函数；测试 yaw=0,±pi/2,pi 与多个世界向量，通过真实 sim 输入变换还原后 dot(actual,v)>0.99。当前错误在非零yaw时足以朝敌人撤离，不能归因于战斗难度。
2. 同文件两处 attackAvailable:true / canFaceEnemy:true 是假设，不是测量。读取实际 attack phase、player yaw、敌人位置，用生产攻击角度/范围判定；朝向不对先合法转向，但威胁优先打断。不可把默认true传给纯函数伪造前提。
3. 群体windup分支当前即使能闪避也优先普通flee，且没有地形/碰撞安全验证；按上一轮要求：使用生产 canAcceptDodge/状态条件，优先已验证安全方向的移动+闪避；无闪避再用经碰撞验证的撤离。敌人向量互相抵消时不可返回零方向。测试包围对称、冷却/耐力不足、目标方向受阻。不要更改生产敌人/无敌/伤害参数。
4. 只在局部验证上述动作方向、实际攻击/闪避被接受与首伤轨迹后，再一次从自然完成档尝试牵引。不以 recover 快照就判定受伤合理；它可能是攻击结束后的状态。保留首次伤害前因，诊断首次扣血默认停止。已自然获得的完成档不得被失败覆盖。
5. 当前范围仍是四项玩家体验，不是无限扩展自动战斗AI；若可用普通输入沿已验证安全路径避开营地，优先完成牵引局部验证，再继续 C05 指引/祠内机制/still 等，不能仅硬编码脚本成功就验收导航。同步STATUS实际测试数/PID，不阶段总结停工；所有原权限边界保持。
